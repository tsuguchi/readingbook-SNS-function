# 読書管理SNS 技術スタック選定書

本書は [要件定義.md](要件定義.md) を実現するための技術選定をまとめたものです。

---

## 0. サマリ

| レイヤー | 採用技術 |
|---|---|
| フロントエンド | **Next.js (App Router) + TypeScript + Tailwind CSS + shadcn/ui** |
| バックエンド | **Ruby on Rails (API モード) + Ruby** |
| データベース | **PostgreSQL** |
| 全文検索 | **PostgreSQL 全文検索（pg_search gem）** |
| 認証 | **Devise + devise-jwt（JWT 認証）** |
| 画像ストレージ | **Active Storage**（本番: AWS S3 / 開発: MinIO） |
| キャッシュ・ジョブキュー | **Redis + Sidekiq** |
| プッシュ通知 | **Firebase Cloud Messaging (FCM)** + Web Push |
| API スタイル | **REST (JSON API)** |
| 開発環境 | **Docker + Docker Compose**（Windows + WSL2 推奨） |
| クラウド | **AWS**（ECS Fargate / RDS / ElastiCache / S3 / CloudFront） |
| CI/CD | **GitHub Actions** |
| バージョン管理 | **Git / GitHub** |

---

## 1. フロントエンド

### 1.1 採用技術

| 種別 | 採用 | バージョン目安 |
|---|---|---|
| フレームワーク | **Next.js (App Router)** | 15.x |
| 言語 | **TypeScript** | 5.x |
| パッケージマネージャ | **pnpm**（高速・ディスク効率良） | 9.x |
| Node.js | LTS | 20.x or 22.x |
| スタイリング | **Tailwind CSS** | 3.x |
| UI コンポーネント | **shadcn/ui**（Radix UI ベース、コピペ式） | 最新 |
| データ取得 | **TanStack Query (React Query)** | 5.x |
| フォーム | **React Hook Form + Zod** | 最新 |
| HTTP クライアント | `fetch` ラッパー（自前 or `ky`） | - |
| アイコン | **lucide-react** | 最新 |
| 日付処理 | **date-fns** | 3.x |
| Lint / Format | **ESLint + Prettier**（typescript-eslint, eslint-plugin-tailwindcss） | 最新 |
| テスト | **Vitest + Testing Library + Playwright (E2E)** | 最新 |

### 1.2 採用理由

- **Next.js (App Router)**: SSR/SSG/ISR と CSR を柔軟に使い分け可能。SEO と LCP の両立に強い。
- **shadcn/ui**: ライブラリではなくコードコピー方式のため、カスタマイズ自由・依存膨張なし。Radix UI のアクセシビリティ恩恵を享受できる。
- **TanStack Query**: いいね件数のキャッシュ更新やオプティミスティックUIに最適。
- **Zod**: 入力バリデーションと型推論を一元化。フロント・API レスポンス両方で型安全に使える。

### 1.3 ディレクトリ構成（例）

```
frontend/
├── app/                    # Next.js App Router
│   ├── (auth)/             # 認証関連ルート
│   ├── (main)/             # 認証必須エリア
│   │   ├── timeline/
│   │   ├── posts/[id]/
│   │   ├── books/[id]/
│   │   ├── users/[handle]/
│   │   ├── search/
│   │   └── notifications/
│   └── layout.tsx
├── components/
│   ├── ui/                 # shadcn/ui コンポーネント
│   └── features/           # 機能別コンポーネント
├── lib/
│   ├── api/                # API クライアント
│   ├── auth/
│   └── utils/
├── hooks/
└── types/
```

---

## 2. バックエンド

### 2.1 採用技術

| 種別 | 採用 | バージョン目安 |
|---|---|---|
| フレームワーク | **Ruby on Rails (API モード)** | 7.2.x or 8.0.x |
| 言語 | **Ruby** | 3.3.x |
| Web サーバ | **Puma** | 6.x |
| データベース | **PostgreSQL** | 16.x |
| 認証 | **Devise + devise-jwt** | 最新 |
| 認可 | **Pundit** | 最新 |
| 全文検索 | **pg_search**（PostgreSQL を直接活用） | 最新 |
| ジョブキュー | **Sidekiq + Redis** | 最新 |
| ファイル添付 | **Active Storage**（S3 / MinIO） | Rails 標準 |
| ページネーション | **Pagy** | 最新 |
| シリアライザー | **JSON:API Serializer**（jsonapi-serializer）or **Active Model Serializers** | 最新 |
| Push 通知 | **fcm gem**（Firebase Cloud Messaging）+ **webpush gem** | 最新 |
| API ドキュメント | **rswag**（OpenAPI 3.0） | 最新 |
| Lint / Format | **RuboCop（rubocop-rails / rubocop-rspec）** | 最新 |
| テスト | **RSpec + FactoryBot + faker** | 最新 |
| 環境変数 | **dotenv-rails** | 最新 |

### 2.2 採用理由

- **Rails API モード**: ビューや Asset Pipeline を切り、JSON API に専念。Next.js とのデカップル運用に適合。
- **Devise + devise-jwt**: Rails 認証のデファクト。JWT で Next.js と疎結合に連携可能。
- **pg_search**: PostgreSQL の `tsvector` / `tsquery` を Rails 流に隠蔽。MVP 段階で別途 ES を立てずに済む。日本語は `pg_bigm` または `textsearch_ja` 拡張で対応。
- **Sidekiq**: 通知配送・タイムライン Fan-out・画像変換などの非同期処理に必須。

### 2.3 ディレクトリ構成（一部）

```
backend/
├── app/
│   ├── controllers/
│   │   └── api/v1/         # API バージョニング
│   ├── models/
│   ├── serializers/
│   ├── policies/           # Pundit
│   ├── services/           # ドメインロジック
│   └── workers/            # Sidekiq ジョブ
├── config/
├── db/
│   └── migrate/
├── spec/
└── Dockerfile
```

### 2.4 主要 Gem 一覧

```ruby
# Gemfile（抜粋）
gem 'rails', '~> 7.2'
gem 'pg'
gem 'puma'
gem 'devise'
gem 'devise-jwt'
gem 'pundit'
gem 'pg_search'
gem 'sidekiq'
gem 'redis'
gem 'aws-sdk-s3'
gem 'image_processing'
gem 'jsonapi-serializer'
gem 'pagy'
gem 'fcm'
gem 'webpush'
gem 'rack-cors'

group :development, :test do
  gem 'rspec-rails'
  gem 'factory_bot_rails'
  gem 'faker'
  gem 'rubocop-rails'
  gem 'rubocop-rspec'
  gem 'dotenv-rails'
  gem 'rswag-specs'
end
```

---

## 3. データベース

### 3.1 PostgreSQL

- バージョン: **16.x**
- 拡張: `pgcrypto`, `citext`（メール大小無視）, `pg_bigm` または `pg_trgm`（全文検索の日本語サポート）

### 3.2 全文検索戦略

| 対象 | 手法 |
|---|---|
| ユーザー検索 | `ILIKE` + `pg_trgm` インデックスで前方一致・部分一致 |
| 本検索 | `ILIKE` + `pg_trgm`（タイトル・著者）、ISBN は完全一致 |
| 投稿本文検索 | `pg_search` + `tsvector` または `pg_bigm`（日本語 N-gram） |
| ハッシュタグ | 別テーブル `hashtags` に正規化、`name` UNIQUE インデックス |

### 3.3 マイグレーション戦略

- Rails 標準 `db/migrate/` を使用
- 索引（インデックス）は要件 LK-03（ユニーク制約）、検索性能を踏まえ計画的に付与

---

## 4. 認証・認可

### 4.1 認証フロー

1. ユーザーがメール＋パスワードでサインアップ／ログイン → Rails (Devise) が JWT を発行
2. Next.js は JWT を **httpOnly Cookie** に保存（XSS リスク低減）
3. 以降の API 呼び出しは Cookie 経由で JWT を送付（または `Authorization: Bearer`）
4. JWT 失効・リフレッシュは `devise-jwt` の Denylist 戦略を採用

### 4.2 認可

- **Pundit** によるポリシークラスを各リソースに対し定義
- 例: 非公開アカウントの投稿は所有者またはフォロワーのみ閲覧可

---

## 5. 画像ストレージ

### 5.1 構成

| 環境 | ストレージ |
|---|---|
| ローカル開発 | **MinIO**（Docker コンテナ、S3 API 互換） |
| 本番 | **AWS S3**（CloudFront 経由配信） |

Rails は **Active Storage** で接続先を `config/storage.yml` で切替。コードは環境差を意識しない。

### 5.2 画像処理

- `image_processing` gem + libvips（Active Storage variant）
- アイコンは複数解像度（small/medium/large）を生成
- 巨大画像はアップロード時にリサイズ・形式統一（WebP）

---

## 6. 非同期処理

### 6.1 Sidekiq ジョブ

| ジョブ名 | 用途 |
|---|---|
| `LikeNotificationWorker` | いいね発生時の通知配信（集約処理含む） |
| `FollowNotificationWorker` | フォロー / フォローリクエスト通知 |
| `RepostNotificationWorker` | リポスト通知 |
| `TimelineFanoutWorker` | 投稿時のフォロワータイムラインへの配信 |
| `SearchIndexWorker` | 検索インデックス更新（`tsvector` 列の再計算） |
| `BlockCleanupWorker` | ブロック成立時のフォロー / いいね削除 |
| `ImageVariantWorker` | 画像 variant 生成 |

### 6.2 キュー設計

- 優先度別キュー: `critical`（通知）, `default`, `low`（インデックス、画像）

---

## 7. プッシュ通知

| 対象 | 仕組み |
|---|---|
| iOS / Android アプリ（将来） | **Firebase Cloud Messaging (FCM)** |
| Web ブラウザ | **Web Push (VAPID)** |

- デバイストークンは `Device` テーブルに保存（ユーザー単位で複数台登録可）
- 通知設定はプロフィール拡張で管理（範囲外と既定だが将来対応）

---

## 8. Docker（Windows 開発環境）

### 8.1 構成

`docker-compose.yml`（概要）:

```yaml
services:
  frontend:    # Next.js
    build: ./frontend
    ports: ["3000:3000"]
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
    environment:
      - NEXT_PUBLIC_API_BASE=http://localhost:3001/api/v1

  backend:     # Rails API
    build: ./backend
    ports: ["3001:3000"]
    volumes:
      - ./backend:/app
      - backend_bundle:/usr/local/bundle
    depends_on: [postgres, redis, minio]

  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: password
    volumes: [postgres_data:/var/lib/postgresql/data]
    ports: ["5432:5432"]

  redis:
    image: redis:7
    ports: ["6379:6379"]

  sidekiq:
    build: ./backend
    command: bundle exec sidekiq
    depends_on: [postgres, redis]

  minio:       # S3 互換ローカルストレージ
    image: minio/minio
    command: server /data --console-address ":9001"
    ports: ["9000:9000", "9001:9001"]
    volumes: [minio_data:/data]

volumes:
  postgres_data:
  minio_data:
  frontend_node_modules:
  backend_bundle:
```

### 8.2 Windows 固有の注意点

| 項目 | 推奨対応 |
|---|---|
| Docker 実行基盤 | **WSL2 バックエンド** を必ず有効化（Hyper-V 単体だと激遅） |
| ソースコード配置 | **WSL2 内のファイルシステム**（`\\wsl$\...` または `/home/<user>/...`）に置く。Windows 側 (`C:\`) に置くとボリューム I/O が極端に遅い |
| 改行コード | プロジェクトに `.gitattributes` を置き `* text=auto eol=lf` で LF 統一。`.sh` `Dockerfile` 等は明示的に LF 指定 |
| `node_modules` / `bundle` | ホストではなく **named volume** に置く（バインドマウントだと激遅） |
| ファイル監視（HMR） | Next.js の Dev Server は `WATCHPACK_POLLING=true` または `CHOKIDAR_USEPOLLING=true` を環境変数で指定すると安定 |
| Rails キャッシュ | `bootsnap` キャッシュも named volume に置く |
| ポート競合 | Windows の他サービス（IIS / WSL の Docker Desktop）と 3000・5432 が衝突しがち。`.env` でポートを変えられるよう設計 |

### 8.3 ローカル起動手順

```powershell
# 初回
docker compose build
docker compose run --rm backend bundle exec rails db:setup
docker compose run --rm frontend pnpm install

# 日常
docker compose up
```

---

## 9. AWS 本番構成（想定）

```
┌─────────────────────────────────────────────────────────┐
│                       Route 53 (DNS)                     │
└────────────────────────┬────────────────────────────────┘
                         │
         ┌───────────────┴───────────────┐
         ▼                               ▼
   CloudFront (Web)                 CloudFront (Image CDN)
         │                               │
         ▼                               ▼
   ALB (Application LB)              S3 (画像)
         │
   ┌─────┴─────┐
   ▼           ▼
ECS Fargate  ECS Fargate
(Next.js)    (Rails API)
                │
                ├──→ RDS PostgreSQL (Multi-AZ)
                ├──→ ElastiCache Redis
                └──→ Sidekiq タスク (ECS Fargate)
```

| AWS サービス | 用途 |
|---|---|
| ECS Fargate | コンテナ実行（Next.js / Rails / Sidekiq） |
| ECR | Docker イメージレジストリ |
| RDS for PostgreSQL | 本番 DB（Multi-AZ 推奨） |
| ElastiCache (Redis) | Sidekiq + キャッシュ |
| S3 | 画像保管 |
| CloudFront | CDN（画像・静的コンテンツ） |
| Route 53 | DNS |
| ACM | SSL 証明書 |
| Secrets Manager | DB パスワード・JWT 秘密鍵 |
| CloudWatch | ロギング・メトリクス |
| SES | メール送信（パスワードリセット等、将来） |

---

## 10. CI/CD

| ツール | 用途 |
|---|---|
| **GitHub Actions** | PR ごとに lint / test / build、main マージで ECR への push & ECS デプロイ |
| **Renovate / Dependabot** | 依存ライブラリ自動更新 |

代表的なワークフロー:

1. **lint-test.yml**: PR 時に `pnpm lint && pnpm test`、`bundle exec rubocop && bundle exec rspec`
2. **build-deploy.yml**: main push 時に Docker build → ECR push → ECS service update

---

## 11. 監視・可観測性

| 項目 | ツール |
|---|---|
| エラー監視 | **Sentry**（フロント・バック共通） |
| APM | AWS CloudWatch / Datadog（規模に応じて） |
| ログ集約 | CloudWatch Logs |
| メトリクス | CloudWatch Metrics、必要なら Prometheus + Grafana |

---

## 12. 開発フロー

| 項目 | 採用 |
|---|---|
| ブランチ戦略 | GitHub Flow（main + feature ブランチ） |
| PR レビュー | 必須、CI 緑必須 |
| コミット規約 | Conventional Commits 推奨（`feat:`, `fix:` 等） |
| 環境変数管理 | `.env`（dev）、AWS Secrets Manager（prod） |
| ドキュメント | リポジトリ内 Markdown、API は OpenAPI（rswag） |

---

## 13. 段階的導入計画（Phase 提案）

| Phase | 範囲 |
|---|---|
| Phase 1 (MVP) | ユーザー登録/ログイン、投稿、いいね、フォロー、タイムライン、プロフィール |
| Phase 2 | 検索（PG 全文検索）、リポスト、ブロック・ミュート、通知（ベル） |
| Phase 3 | プッシュ通知、Web Push、画像周り強化 |
| Phase 4 | スケール対策（必要に応じて Elasticsearch 移行検討） |

---

## 14. 範囲外・将来検討

- 検索が PG で頭打ちになったら **Elasticsearch / OpenSearch** に切替
- ネイティブモバイルアプリを作るなら **React Native (Expo)** を Next.js とコード共有
- リアルタイム通知（WebSocket / Action Cable）
- GraphQL の導入（Phase 4 以降）
