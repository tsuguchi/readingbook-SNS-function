# 読書管理SNS 要件定義書（いいね・フォロー・リポスト・検索・プロフィール）

## Context

読書管理した記録（本・読書感想・レビュー等）を他ユーザーと共有できるSNSアプリケーションを **新規開発** する。本ドキュメントは、その中核的なソーシャル機能 5 種類（いいね / フォロー / リポスト / 検索 / プロフィール設定）に関する要件定義書である。

- 対象: 完全新規プロジェクト
- 技術スタック: 未定（要件は技術中立的に記述する）
- 想定ユーザー: 本を読むのが好きな個人ユーザー（ログイン必須）

---

## 1. 共通要件

### 1.1 用語定義

| 用語 | 定義 |
|---|---|
| ユーザー | アプリにアカウント登録した利用者 |
| 投稿 | ユーザーが書いた読書感想・レビュー・読書記録 |
| 本 | 書籍マスタに登録された書籍エンティティ（タイトル・著者・ISBN等を持つ） |
| コメント | 投稿に対する他ユーザーの返信 |
| タイムライン | フォロー中ユーザーの投稿・リポストが時系列で並ぶフィード |
| 公開アカウント | 誰でも投稿が閲覧でき、フォロー承認不要なアカウント |
| 非公開アカウント | フォロワーのみが投稿を閲覧でき、フォローに承認が必要なアカウント |

### 1.2 共通制約

- **認証**: 全ソーシャル操作（いいね・フォロー・リポスト・コメント等）は **ログインユーザーのみ** 実行可。閲覧は公開設定に従う。
- **ブロック関係**: ブロックしている／されているユーザーに対しては、いいね・フォロー・リポスト・コメント・検索結果での相互可視を不可とする。
- **ミュート関係**: ミュートしているユーザーの投稿・リポストはタイムラインから除外する（相手側にミュート事実は通知しない）。
- **削除済み投稿**: 元投稿が削除された場合、その投稿に紐づくいいね・コメント・リポストは表示停止または論理削除とする。

---

## 2. いいね機能

### 2.1 機能概要

ユーザーが投稿・本・コメントに対して肯定的な反応を残せる機能。

### 2.2 対象オブジェクト

| 対象 | 概要 |
|---|---|
| 投稿（読書感想・レビュー） | 他ユーザーが書いた感想・レビュー・読書記録 |
| 本（書籍） | 書籍マスタに登録された本そのもの（「気になる本」的な意味合いで使用可） |
| コメント | 投稿に対するコメント |

### 2.3 機能要件

| ID | 要件 |
|---|---|
| LK-01 | ログインユーザーは対象オブジェクトに対していいねを付与できる。 |
| LK-02 | いいねは **トグル式**: 既にいいね済みの対象に再度操作するといいねが解除される。 |
| LK-03 | 同一ユーザー × 同一対象の組み合わせは **最大 1 件** とする（DB ユニーク制約推奨）。 |
| LK-04 | いいねの **件数のみが公開** される。誰がいいねしたかの一覧は他ユーザーから参照不可。 |
| LK-05 | 自分が押したいいねは自分のマイページ等で確認できる（操作主体本人にのみ可視）。 |
| LK-06 | 自分の投稿に対するいいねは投稿者本人が「誰からされたか」を確認できる（投稿者特権）。※他ユーザーには非公開のまま。 |
| LK-07 | いいね件数はキャッシュ可能とし、リアルタイム性は緩く（数秒〜数十秒の遅延を許容）してもよい。 |
| LK-08 | ブロック関係にあるユーザーへのいいねは付与できない。既存いいねは関係成立後に件数から除外する。 |

### 2.4 通知要件

| ID | 要件 |
|---|---|
| LK-N-01 | 対象オブジェクトの所有者（投稿者・コメント者）に対し **アプリ内通知（ベルアイコン）** を送信する。 |
| LK-N-02 | 同上に対し **プッシュ通知**（モバイル/Webプッシュ）を送信する。送信可否はユーザーのプッシュ設定に従う。 |
| LK-N-03 | 短時間に多数のいいねが来た場合は **集約通知**（例: 「Aさん他5人がいいねしました」）を許容する。 |
| LK-N-04 | 自分自身のいいねでは通知を発生させない。 |
| LK-N-05 | 「本」へのいいねは通知対象外（所有者が存在しないため）。 |

### 2.5 非機能要件

- 件数表示の整合性は最終的整合（eventual consistency）で可。
- 高頻度操作対策として、連打抑止・短時間連続トグルのデバウンスを実装する。

---

## 3. フォロー機能

### 3.1 機能概要

ユーザー間の購読関係を構築する機能。公開アカウントと非公開アカウントで挙動が異なる。

### 3.2 機能要件

| ID | 要件 |
|---|---|
| FL-01 | 公開アカウントへのフォローは **承認不要**（一方向フォロー / Twitter/X 型）で即時成立する。 |
| FL-02 | 非公開アカウントへのフォローは **フォローリクエスト** となり、相手の承認・拒否が必要（Instagram 型）。 |
| FL-03 | アカウントを「公開→非公開」へ変更した場合、既存のフォロワー関係は維持する。新規のフォローのみリクエスト制となる。 |
| FL-04 | アカウントを「非公開→公開」へ変更した場合、保留中のリクエストは自動承認 **しない**（ユーザーに選択を委ねる）か、設定で挙動を選べることを推奨。 |
| FL-05 | 自分自身をフォローすることはできない。 |
| FL-06 | フォロー / フォロワーの一覧および件数は、対象アカウントが公開の場合のみ閲覧可能。非公開の場合はフォロワーのみ閲覧可能。 |
| FL-07 | フォロー解除はいつでも可能で、相手への通知は行わない（推奨）。 |

### 3.3 ブロック機能

| ID | 要件 |
|---|---|
| BL-01 | ユーザーは任意のユーザーをブロックできる。 |
| BL-02 | ブロックが成立すると、双方のフォロー関係（双方向とも）は自動解除される。 |
| BL-03 | ブロックしたユーザーは、自分の投稿・プロフィールを閲覧できず、いいね・コメント・リポスト・フォロー・検索ヒットも不可となる。 |
| BL-04 | 自分のブロックリストはマイページから管理（追加・解除）できる。 |
| BL-05 | ブロックされた側に「ブロックされた」旨の通知は送らない（ただし関係が消えていることは結果的に伝わる）。 |

### 3.4 ミュート機能

| ID | 要件 |
|---|---|
| MU-01 | ユーザーは任意のユーザーをミュートできる。 |
| MU-02 | ミュート対象ユーザーの投稿・リポストは自分のタイムラインから除外される。 |
| MU-03 | フォロー関係は維持され、相手にもミュート事実は **通知されない**。 |
| MU-04 | 個別投稿への直接アクセス・通知（自分宛のいいね等）は受け取れる。 |

### 3.5 通知要件

| ID | 要件 |
|---|---|
| FL-N-01 | 公開アカウントへのフォロー成立時、フォローされた側にアプリ内通知＋プッシュ通知を送る。 |
| FL-N-02 | 非公開アカウントへのフォローリクエスト発生時、リクエストされた側にアプリ内通知＋プッシュ通知を送る。 |
| FL-N-03 | リクエスト承認時、リクエスト発信者にアプリ内通知＋プッシュ通知を送る。リクエスト拒否時は通知を送らない（推奨）。 |

---

## 4. リポスト機能

### 4.1 機能概要

他ユーザーの投稿を自分のフォロワーへ拡散する機能。

### 4.2 機能要件

| ID | 要件 |
|---|---|
| RP-01 | **単純リポスト**: 元投稿をそのまま自分のタイムライン／フォロワーのタイムラインに流す。 |
| RP-02 | **引用リポスト**: 自分のコメント（テキスト）を付けて元投稿を引用する。引用部分は元投稿の埋め込みとして表示する。 |
| RP-03 | リポストは **トグル式**: もう一度操作すると単純リポストは取り消される（引用リポストは「投稿削除」相当の操作で取り消し）。 |
| RP-04 | 元投稿に **リポスト件数** が表示される（単純＋引用の合算、または分けて表示）。 |
| RP-05 | 元投稿の詳細画面から **リポストしたユーザー一覧** を閲覧できる。 |
| RP-06 | 自分の投稿を自分でリポストすることは可能 / 不可能、いずれかをプロダクト側で選定（推奨: 不可）。 |
| RP-07 | 元投稿が削除された場合、関連する単純リポストは表示停止、引用リポストは「元投稿は削除されました」プレースホルダ表示とする。 |
| RP-08 | 非公開アカウントの投稿は **リポスト不可**（プライバシー保護のため）。 |
| RP-09 | ブロック関係にあるユーザーの投稿はリポストできない。 |

### 4.3 通知要件

| ID | 要件 |
|---|---|
| RP-N-01 | 元投稿者に対し、リポスト発生時にアプリ内通知＋プッシュ通知を送る。 |
| RP-N-02 | 引用リポストの場合は、引用コメント本文も通知に含める（要約可）。 |
| RP-N-03 | 短時間に多数のリポストが発生した場合は集約通知を許容する。 |

---

## 5. 検索機能

### 5.1 機能概要

ユーザー・本・投稿・タグを横断的に検索する機能。

### 5.2 検索対象とフィールド

| 検索カテゴリ | 検索キー | 備考 |
|---|---|---|
| ユーザー | ニックネーム / 表示名 | 部分一致＋前方一致を優先表示 |
| 本 | タイトル / 著者名 / ISBN | ISBN は完全一致優先 |
| 投稿・感想 | 本文キーワード | 全文検索（形態素 or n-gram） |
| ハッシュタグ・ジャンル | `#タグ` / ジャンル名 | タグはユーザー入力、ジャンルはマスタ管理 |

### 5.3 機能要件

| ID | 要件 |
|---|---|
| SR-01 | 検索画面はカテゴリタブ（ユーザー / 本 / 投稿 / タグ）で切り替えられる。または「すべて」タブで横断結果を表示する。 |
| SR-02 | 検索キーワードはスペース区切りで複数指定可能（AND 検索）。 |
| SR-03 | 検索結果は無限スクロールまたはページネーションで表示する。 |
| SR-04 | 非公開アカウントの投稿は本文検索の対象外。プロフィールはヒットするがフォロワー以外には投稿数や投稿一覧は表示しない。 |
| SR-05 | ブロック関係にあるユーザーは互いに検索結果にヒットしない。 |
| SR-06 | 検索履歴はユーザー単位で保存し、削除も可能（プライバシー考慮）。 |
| SR-07 | サジェスト機能（入力中の補完）を提供する。 |
| SR-08 | ハッシュタグ詳細ページから、そのタグが付いた投稿一覧へ遷移できる。 |
| SR-09 | ジャンルはマスタ管理（例: 小説、ビジネス、エッセイ、技術書 等）でフィルタとしても利用可能。 |

### 5.4 非機能要件

- 全文検索バックエンド（Elasticsearch / OpenSearch / Algolia 等）の採用を推奨。具体は設計フェーズで決定。
- 検索レスポンスは P95 で 500ms 以内を目標。

---

## 6. プロフィール設定機能

### 6.1 機能概要

自身のアカウント情報・読書関連の好み・公開範囲を設定する機能。

### 6.2 設定項目

#### 6.2.1 基本情報

| 項目 | 必須 | 制約 |
|---|---|---|
| 表示名 | ◯ | 1〜50 文字 |
| ニックネーム（@ハンドル） | ◯ | 半角英数字＋アンダースコア、3〜20 文字、ユニーク |
| アイコン画像 | △ | 画像（JPEG/PNG/WebP）、最大サイズ規定あり、未設定時はデフォルト画像 |
| 自己紹介 | △ | 0〜200 文字、改行可 |

#### 6.2.2 読書関連

| 項目 | 概要 |
|---|---|
| 好きなジャンル | ジャンルマスタから複数選択 |
| 読書目標 | 年間冊数 / 月間冊数などの数値目標。任意 |
| お気に入りの本 | 書籍マスタから複数選択。プロフィールにピン留め表示 |

#### 6.2.3 公開設定

| 項目 | 概要 |
|---|---|
| アカウント公開 / 非公開 | 公開: 誰でも投稿閲覧可・フォロー即時成立。非公開: フォロワーのみ閲覧可・フォロー承認制。 |

### 6.3 機能要件

| ID | 要件 |
|---|---|
| PR-01 | プロフィール各項目は個別に編集・保存できる。 |
| PR-02 | ニックネームは変更可能だが、変更履歴は内部で保持しなりすまし防止に活用してもよい。 |
| PR-03 | アイコン画像は自動リサイズ・トリミング機能を提供する。 |
| PR-04 | 公開→非公開の切り替えは即時反映する。既存の検索キャッシュ等は最終的整合で可。 |
| PR-05 | プロフィール画面には: アイコン・表示名・@ハンドル・自己紹介・読書関連情報・フォロー/フォロワー数・投稿数・お気に入りの本・最近の投稿、を表示する。 |
| PR-06 | 自身のプロフィール画面からは編集ボタン、他ユーザーからはフォロー/ブロック/ミュート操作ボタンが表示される。 |

### 6.4 範囲外（今回は要件化しない）

- 通知設定、メール変更、パスワード変更などの **アカウント設定** は別機能として今回スコープ外。

---

## 7. 画面構成（概観）

| 画面 | 主要素 |
|---|---|
| タイムライン | フォロー中ユーザーの投稿・リポスト、いいね/コメント/リポスト操作 |
| 投稿詳細 | 本文、いいね件数、コメント、リポスト件数・一覧 |
| 本詳細 | 本のメタ情報、いいね件数、関連投稿一覧 |
| ユーザープロフィール | 基本情報、読書関連、フォロー/フォロワー、投稿一覧 |
| 検索 | カテゴリタブ、検索結果、サジェスト |
| 通知 | いいね・フォロー・リポスト・コメントの統合通知一覧 |
| 設定 | プロフィール編集、公開設定、ブロック/ミュート管理 |

---

## 8. データモデル概要（論理設計レベル）

```
User           (id, handle, display_name, avatar_url, bio, is_private, ...)
UserProfile    (user_id, favorite_genres[], reading_goal, favorite_books[])
Book           (id, title, author, isbn, ...)
Post           (id, user_id, body, book_id?, created_at, ...)
Comment        (id, post_id, user_id, body, created_at)
Like           (id, user_id, target_type[POST|BOOK|COMMENT], target_id, created_at) UNIQUE(user_id,target_type,target_id)
Follow         (id, follower_id, followee_id, status[PENDING|ACCEPTED], created_at) UNIQUE(follower_id,followee_id)
Block          (id, blocker_id, blocked_id, created_at) UNIQUE
Mute           (id, muter_id, muted_id, created_at) UNIQUE
Repost         (id, user_id, post_id, comment?, type[SIMPLE|QUOTE], created_at) UNIQUE(user_id,post_id,type=SIMPLE)
Hashtag        (id, name UNIQUE)
PostHashtag    (post_id, hashtag_id)
Notification   (id, recipient_id, actor_id, type, target_type, target_id, read_at?, created_at)
SearchHistory  (id, user_id, query, executed_at)
```

---

## 9. 非機能要件（横断）

| カテゴリ | 要件 |
|---|---|
| 性能 | タイムライン取得 P95 1.0s 以内、検索 P95 500ms 以内（指標目安） |
| スケーラビリティ | いいね・リポストはホットスポット化しやすいためカウンタ集計をキャッシュ／非同期化 |
| 可観測性 | ソーシャル操作のメトリクス（DAU、いいね数、フォロー数、検索回数 等）を計測 |
| セキュリティ | レート制限（連続いいね、連続フォロー）、スパム検出、不正アクセス監視 |
| プライバシー | 非公開アカウント・ブロック関係を全機能で確実に尊重する。検索インデックスにも反映 |
| アクセシビリティ | スクリーンリーダー対応、いいねボタン等のARIA属性 |

---

## 10. 範囲外 / 将来拡張候補

- いいね一覧の公開、誰がいいねしたかを公開する設定
- いいね数ランキング、トレンド機能
- DM（ダイレクトメッセージ）
- 通報・モデレーション機能
- 読書記録としての「読了日」「★レーティング」などの拡張
- 推薦エンジン（フォロー推薦、本の推薦）

---

## 11. 検証方法（要件レベル）

設計・実装後に以下のシナリオで E2E 検証する。

- [ ] 公開アカウント A が公開アカウント B をフォロー → 即時フォロワーになる
- [ ] 公開アカウント A が非公開アカウント C をフォロー → リクエスト保留 → C が承認 → フォロワーになる
- [ ] 投稿にいいね → 件数+1、再度いいね → 件数-1（トグル）
- [ ] いいねされた側にプッシュ通知＋ベル通知が届く
- [ ] 単純リポスト → フォロワーのタイムラインに表示
- [ ] 引用リポスト → コメントとともに表示、元投稿者に通知
- [ ] 非公開アカウントの投稿はリポスト操作で拒否される
- [ ] ユーザー A が B をブロック → 相互の検索ヒット消失、フォロー関係解除
- [ ] ユーザー A が B をミュート → A のタイムラインから B の投稿が消える、B には伝わらない
- [ ] 検索: ユーザー / 本 / 投稿 / タグ それぞれのカテゴリで結果が返る
- [ ] プロフィール公開→非公開へ切り替え → 既存フォロワー以外から投稿が見えなくなる
- [ ] お気に入りの本がプロフィールに表示される

---

## 12. 想定アウトプット（次工程）

本要件定義をベースに、次フェーズで以下を作成する想定:

1. **画面遷移図 / ワイヤーフレーム**
2. **ER 図・API 設計**
3. **技術スタック選定書**（Web SPA / モバイル / バックエンド / 検索基盤）
4. **テスト計画書**
